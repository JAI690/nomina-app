<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido </h3>
                    <h4>{{user.nombre}}</h4>
                    <h4>ID: {{user.id}}</h4>
                    <a href="/links/mios" class="btn btn-primary m-4">
                        Mis candidatos
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>